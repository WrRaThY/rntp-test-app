repo to replicate a bug in RNTP

steps that generated this repo:
```shell
npx create-expo-app rntp-test-app
npx expo install react-native-track-player
expo run:android
```

which resulted in
![img.png](img.png)